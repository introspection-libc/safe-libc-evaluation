#!/bin/sh
COUNT=30
SIZE=$((1024 * 64))

echo "$COUNT iterations with size $SIZE..."

echo "Normal:"
for i in `seq $COUNT`; do
	./strlen-normal $SIZE
done

echo "Instrumented:"
for i in `seq $COUNT`; do
	./strlen $SIZE
done
